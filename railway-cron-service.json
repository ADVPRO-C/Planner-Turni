{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install"
  },
  "deploy": {
    "startCommand": "cd server && node scripts/cleanup-disponibilita.js --before-current-month",
    "restartPolicyType": "NEVER"
  }
}

